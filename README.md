# AI業務教練｜豪老師 Hao+

以語音角色扮演為核心的 AI 業務訓練工具，協助保險業務人員反覆練習邀約、需求探索、
商品說明與異議處理，並在每次演練後得到五項能力評分與教練回饋。

**👉 直接使用：https://zjh0511.github.io/ai-sales-coach/**

> ⚠️ 本工具僅供自學訓練參考，請勿公開分享生成內容。
> 業務員網路言行不得涉及招攬保險或招募行為。
> 本工具不提供法律、稅務、醫療或財務建議。

---

## 六大功能

| # | 功能 | 說明 |
|---|---|---|
| 1 | 客戶潛在痛點分析 | 輸入客戶背景 → 三個潛在痛點（含推測原因、需求方向、可直接問出口的探索問題）＋ 建議接觸方式 |
| 2 | 電話邀約語音對練 | AI 扮演客戶進行語音通話演練 → 五項評分 ＋ 教練回饋 |
| 3 | 發掘需求角色扮演 | 客戶有隱藏需求，只有問對問題才會鬆口；畫面即時顯示挖到幾項 |
| 4 | 商品行銷語音演練 | 上傳公司教材 → 自動整理 FABE 賣點與常見疑慮 → 針對該商品演練 |
| 5 | 客戶理賠諮詢建議 | 上傳保單條款 → 描述客戶狀況 → 列出可能給付項目、金額、依據條文 |
| 6 | 客戶行銷諮詢建議 | 與教練討論真實客戶狀況，違規建議會被當場糾正 |

---

## 沒有伺服器

這是一個**純前端**應用程式。所有運算都在你自己的瀏覽器裡完成，
瀏覽器直接呼叫你所選的 AI 服務商，中間不經過任何第三方伺服器。

因此：

- **沒有冷啟動**，打開就能用
- **金鑰只存在你的裝置**，不會傳給任何人
- **上傳的文件存在你自己的瀏覽器**（IndexedDB），永久保留，不會因為服務重啟而消失
- **訓練紀錄預設也存在本機**

### 選配：帳號同步

如果專案擁有者設定了 Firebase（見 [account-setup.html](docs/account-setup.html)），
會多出一個註冊／登入畫面，可以用 **Google 帳號**或 **E-mail** 登入，
**訓練紀錄與偏好設定**就會跟著人跑，換手機、換電腦都接得起來。

- **API 金鑰、上傳的教材與保單條款永遠不上雲端**，這是程式碼層面的限制，不是設定選項
- 開啟同步後，訓練紀錄會經過 Google 的伺服器；資料庫擁有者在後台看得到，**這一點應事先告知使用者**
- 登入畫面永遠有「先不要登入，直接使用」的出口，不登入功能完全不受影響
- Apple 登入需要付費的 Apple Developer 帳號（Apple 的規定），程式碼已備好但預設關閉
- 沒有設定 Firebase 時，登入畫面不會出現，App 行為與加入這個功能之前完全相同

---

## 開始使用

### 1. 申請一把免費的 AI 金鑰

1. 前往 https://aistudio.google.com/apikey
2. 用 Google 帳號登入 → **Create API key**
3. 複製那一長串金鑰

### 2. 打開網站並登入

https://zjh0511.github.io/ai-sales-coach/

服務商選「Google Gemini」，把金鑰貼上去，按「驗證並登入」。

### 3. 加入主畫面（建議）

這是一個 **PWA**，可以裝到桌面當成獨立 App 使用：

- **iPhone／iPad**：Safari 開啟 → 分享鍵 → 「加入主畫面」
- **Android／桌面 Chrome**：首頁下方會出現「加到主畫面」按鈕，或用網址列的安裝圖示

裝好之後從桌面圖示開啟是全螢幕、沒有網址列，長按圖示還有捷徑可以直接跳到
電話邀約／發掘需求／痛點分析。

**離線時介面打得開，但演練需要網路**——AI 回應必須連到服務商的 API。

### 不會用 API 金鑰？

登入畫面上有【📖 Google AI Studio，免費 API 申請教學】，
四個步驟＋六個常見問題，全程免費、不需要信用卡。
也可以直接開：[guide.html](https://zjh0511.github.io/ai-sales-coach/guide.html)

---

## 支援的 AI 服務商

| 服務商 | 免費額度 | 可直接讀 PDF | 一鍵登入 | 狀態 |
|---|:--:|:--:|:--:|---|
| Google Gemini | 有 | ✅ | — | **已實測，建議首選** |
| OpenRouter | 有免費模型 | ✗ | ✅ | **已實測**，一把金鑰可用 340+ 模型 |
| Anthropic Claude | 無 | ✅ | — | 未實測 |
| OpenAI | 無 | ✗ | — | 未實測 |
| Groq | 有 | ✗ | — | 未實測 |
| DeepSeek | 無 | ✗ | — | 未實測 |

**OpenRouter 支援一鍵登入**：選 OpenRouter 後會出現「用 OpenRouter 帳號登入」按鈕，
授權後自動取得金鑰，不必手動複製貼上。用的是 OAuth PKCE，全程在瀏覽器完成，不需要伺服器。

> OpenRouter 帳戶若沒有儲值，只能使用名稱結尾為 `:free` 的免費模型（在「模型設定」裡有「免費」標記）。
> 免費模型實測可用但較慢（約 19 秒），且部分模型的繁體中文品質不穩定。

登入時會實際向服務商驗證金鑰，有問題當場就會告訴你。
不支援讀 PDF 的服務商，教材與條款請改上傳 `.docx` / `.pptx` / `.txt`。

### 模型設定

登入後，首頁下方的「模型設定」會顯示目前使用的模型，點進去可以更換。
清單是用你這把金鑰**即時查詢**出來的，不同帳號看到的不一樣。

- 預設是**自動**，不確定選哪個就別動它。
- 推薦的模型排在最前面並標 ★；免費模型標 **(Free)**。
- 模型超過 30 個時會出現搜尋框（OpenRouter 有 300 多個）。

**自動模式的內部行為**：角色扮演會挑最快的模型（客戶只講 1～3 句，小模型就夠），
評分與分析會挑最準的。這是刻意分開的（規格 §47 Judge Model 分離），但使用者不需要知道——
一旦你指定了某個模型，全部功能都會改用它。

**指定模型不會關掉自動降階。** 若模型額度用盡（HTTP 429），系統會：
自動改用下一個可用模型 → 畫面提示你發生了什麼 → 十分鐘後回頭嘗試原本的模型。
練習不會中斷，也不會默默變差而你不知道。

> ⚠️ 只在你信任的網址輸入金鑰。任何人要求你把金鑰貼給他，都應該拒絕。
> 共用裝置請記得使用完登出（首頁下方「登出／更換金鑰」）。

---

## 在自己電腦上開發

### 需要

- [Node.js](https://nodejs.org) 20 以上
- Git for Windows（產生憑證用，內含 openssl）

```bash
git clone https://github.com/zjh0511/ai-sales-coach.git
cd ai-sales-coach
node tools/gencert.mjs      # 產生本機 HTTPS 憑證（手機用麥克風必須走 HTTPS）
node tools/serve.mjs        # 啟動本機靜態伺服器
```

| 用途 | 網址 |
|---|---|
| 電腦除錯（免憑證） | `http://localhost:8444` |
| 手機（需同一個 Wi-Fi） | `https://<你的區網IP>:8443` |

`tools/serve.mjs` 只是個靜態檔案伺服器，正式站不需要它。

### 自我測試

```bash
node tools/selftest.mjs        # 跑完六大功能共 60 項檢查
node tools/selftest.mjs 5      # 只跑第 5 節
```

需要一把 Gemini 金鑰，放在專案根目錄的 `Gemini API Key.txt`，
或設定環境變數 `GEMINI_API_KEY`。這兩者都在 `.gitignore` 內。

---

## 文件

| 文件 | 內容 |
|---|---|
| **[MD檔/11_IMPLEMENTATION_HANDBOOK.md](MD檔/11_IMPLEMENTATION_HANDBOOK.md)** | **V1 實作總結與延伸開發指南**——核心設計概念、踩過的坑與解法、原生 App 與本地模型的分階段計畫。**要接手或延伸開發請先讀這份。** |
| [MD檔/01～04](MD檔/) | 產品規格、AI Coach Engine、模型架構、語音引擎（設計基準） |
| [project/DECISION_LOG.md](project/DECISION_LOG.md) | D001～D020 決策紀錄，含當下理由與實測依據 |
| [SHARE.md](SHARE.md) | 可直接複製給同事的使用說明 |

## 架構

```
docs/                  ← GitHub Pages 網站根目錄
  index.html
  app.js               畫面流程
  guide.html           API 金鑰申請教學
  account-setup.html   帳號同步設定教學（給專案擁有者）
  firebase-config.js   帳號同步設定值（留空＝關閉帳號功能）
  sw.js                Service Worker（PWA，network-first）
  manifest.webmanifest PWA 設定：圖示與桌面捷徑
  voice.js             Voice Engine（STT/TTS 抽象層，可整檔替換 Provider）
  engine/
    api.js             本地 API 層（與舊伺服器版同介面，可再切回伺服器架構）
    gateway.js         Model Gateway：六家服務商、模型探測、重試、額度降級
    prompts.js         分層提示詞：Base → Mode → Persona → State → Turn
    session.js         Coach Engine 狀態機（三種角色扮演模式）
    advisor.js         痛點分析／理賠諮詢／行銷諮詢
    knowledge.js       Knowledge Engine：文件 → 結構化知識庫
    docx.js            ZIP+XML 解析（DecompressionStream，零外部相依）
    store.js           IndexedDB 儲存層（Node 下自動退回記憶體版）
    account.js         帳號與雲端同步（Firebase REST，無 SDK；未設定時整段關閉）
    compliance.js      Compliance Engine（規則優先，不依賴 LLM 判斷）
tools/
  serve.mjs            本機開發用靜態伺服器
  gencert.mjs          自簽憑證產生器
  selftest.mjs         端到端自我測試
```

**零外部相依**，只用瀏覽器與 Node 內建 API，沒有 `node_modules`。

### 文件解析策略

| 格式 | 做法 |
|---|---|
| `.docx` / `.pptx` | 瀏覽器內解 ZIP 讀 XML（`DecompressionStream`） |
| `.pdf` | 直接把原始檔交給模型讀，中文與掃描版都比自寫解析器可靠 |
| `.txt` / `.md` | 直接讀 |
| `.doc` / `.ppt` | 不支援，請另存成新格式或 PDF |

上傳時做一次「知識結構化」（規格 §41），之後查詢用摘要即可。
理賠查詢因為準確度要求高，會回頭比對條款原文。

**投影片如果大多是圖片，取到的字會很少**——系統會直接警告你，並建議改用 PDF。

### 接觸情境與話術資訊邊界

演練前要先選「接觸情境」，這決定業務員**此刻合理知道客戶什麼**，示範話術只能在這個邊界內設計。
`demoLeaksPrivateInfo()` 會掃描產生出來的話術，若在陌生／轉介紹情境中出現保額、保單、
繳費金額等字樣，自動重新產生。

### 建立信任感（NLP）

示範話術與教練回饋遵循七原則：先跟後帶、語言同步、尊重自主權（給對方退路）、
開場交代「我是誰／為什麼是現在／為什麼是你」、先處理情緒再處理事情、
第一次接觸只求一個小承諾、問句從生活出發而非從錢出發。
依規格 §53，明文禁止用於操控、製造恐懼或假裝熟識。

### 話術中立化

系統產生的示範話術一律不預設保險公司：公司寫「○○人壽」、人名寫「○○」。
不套用於逐字稿、評分證據，以及上傳的條款與教材。

---

### 為什麼沒有 NVIDIA 和「用 ChatGPT 登入」

因為純前端架構下**技術上做不到**，不是沒做。2026-08-17 實機驗證：

| 目標 | 阻礙 |
|---|---|
| NVIDIA NIM | 整個 API 完全不回傳 `access-control-*` 標頭，瀏覽器直連被擋（`Failed to fetch`） |
| 用 ChatGPT 帳號登入 | 登入端點 `auth.openai.com` 允許瀏覽器呼叫，但拿到 token 後真正要用的推論端點 `chatgpt.com/backend-api/codex/*` 擋 CORS |

兩者都必須自架伺服器代轉每一次呼叫，那會失去 GitHub Pages 的所有好處（無冷啟動、永久免費、零維護），
而 ChatGPT 那條路還額外有條款疑慮——Anthropic 與 Google 已於 2026 年 2 月封鎖同類做法。

若日後真的需要，`docs/engine/api.js` 保留了與伺服器版相同的介面，加回代理層只需替換該層。

## 已知限制

1. **本機模型尚未接上。** 目前透過雲端 API。手機瀏覽器分頁記憶體約 1～1.5GB，
   跑不動 2～3GB 的裝置端模型；真正的裝置端推論須等原生 App 階段。Gateway 介面已預留。
2. **Barge-in 是「點擊打斷」而非自動偵測。** iOS Safari 無法在 TTS 播放時穩定同時收音。
3. **未做 Streaming。** 客戶回覆本來就只有 1～3 句，先求穩定。
4. **語音類評分依據有限。** 目前只有逐字稿與時間資訊，沒有音高／音量等聲學特徵，
   提示詞已要求模型不得假裝能精準判讀聲音（規格 §39）。
5. **合規詞庫是起點，不是法規全集。** `engine/compliance.js` 需依最新〈保險業務員管理規則〉
   與所屬公司規範持續校正。
6. **理賠建議只是初步判斷。** 系統只看你上傳的那份條款，不知道客戶實際投保的主約附約組合、
   保額、生效日與批註。實際理賠以保險公司核定為準。
7. **資料不跨裝置同步。** 文件與訓練紀錄都存在瀏覽器，換手機要重新上傳。
   清除瀏覽器資料也會一併清掉。

## 實測數據（2026-08-17，瀏覽器直連 gemini-3.7-flash）

| 項目 | 實測 |
|---|---|
| 登入驗證 | 0.26 秒 |
| 角色扮演每回合 | **1.5 秒** |
| 痛點分析 | 8.1 秒 |
| 建立客戶 | 8.9 秒 |
| PPTX 解析（13 頁） | 0.017 秒 |
| 理賠查詢 | 3～6 秒 |

---

## 授權與免責

© 豪老師 Hao+。本專案原始碼公開供學習與測試參考，保留所有權利；
未另行取得書面同意前，請勿用於商業用途或轉散布為自有產品。

本工具為**業務溝通訓練用途**，不構成保險、法律、稅務、醫療或財務建議。
生成內容可能有誤，實際商品內容、理賠認定與法規適用，一律以保險公司核定及主管機關規定為準。
使用者應自行確認所在地區之法規與所屬公司規範。
