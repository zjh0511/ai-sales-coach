# 部署到雲端（讓同事直接用手機開網址測試）

目標：同事不用裝任何東西，開一個網址、輸入通行碼就能用。

以下用 **Render** 免費方案示範。Railway、Fly.io 等做法類似。

---

## 一次性設定（約 10 分鐘）

### 1. 註冊並連接 GitHub

到 https://render.com 用 GitHub 帳號註冊，授權它讀取這個 repo。

### 2. 建立服務

Render 後台 → **New** → **Blueprint** → 選這個 repo。
它會自動讀取根目錄的 `render.yaml`，不需要手動填 build 指令。

如果你想手動建立（New → Web Service），設定是：

| 欄位 | 值 |
|---|---|
| Root Directory | `app` |
| Build Command | 留空 |
| Start Command | `node server.js` |
| Health Check Path | `/api/health` |

### 3. 填入環境變數

在 Render 服務的 **Environment** 分頁新增：

| 變數 | 值 | 說明 |
|---|---|---|
| `CLOUD` | `1` | 讓伺服器跑純 HTTP，TLS 交給 Render |
| `GEMINI_API_KEY` | 你的金鑰 | **只填在這裡，永遠不要寫進程式碼** |
| `ACCESS_CODE` | 自己取一組 | 團隊通行碼，例如 `haoplus2026` |

### 4. 部署完成

Render 會給你一個網址，例如 `https://ai-sales-coach.onrender.com`。

把網址和通行碼傳給同事，請他們用 **Safari 或 Chrome** 開啟，
然後「加入主畫面」，就會像一個 App。

---

## 給同事的說明（可以直接複製傳給他們）

> 網址：https://你的服務名稱.onrender.com
> 通行碼：（你設定的 ACCESS_CODE）
>
> 1. 用手機瀏覽器打開網址，輸入通行碼。
> 2. iPhone：點下方分享鍵 →「加入主畫面」，之後從桌面圖示開啟。
> 3. 第一次按麥克風時，請允許麥克風權限。
> 4. 語音練習時請盡量在安靜的地方，講完一句話停頓一下讓它辨識。
>
> 提醒：這是訓練工具，內容僅供自學參考，請勿公開分享。
> 請不要輸入客戶的真實姓名、電話或身分證號。

---

## 必須知道的三件事

### 1. 免費方案會休眠

Render 免費方案在 15 分鐘沒人使用後會休眠，下一位使用者要等 **30～60 秒**才會醒來。
同事第一次打開覺得很慢是正常的，重新整理一次就好。付費方案（約 $7/月）沒有這個問題。

### 2. 上傳的文件不會永久保存

免費方案的檔案系統是暫時性的，每次重新部署或休眠喚醒後，
`app/data/` 裡上傳過的商品教材與保單條款**會消失，需要重新上傳**。

訓練紀錄存在每個人自己手機的瀏覽器裡，不受影響。

如果要讓文件永久保存，需要接資料庫或物件儲存，那是下一階段的事。

### 3. 大家共用你的 API 額度

所有同事的每一次演練、每一次評分都算在**你這把金鑰**上。
Gemini 免費方案有每日上限，人多的時候可能會用完（系統會自動降級到較小的模型，不會壞掉，但品質會降）。

如果要正式讓團隊使用，建議：
- 把 Google AI Studio 的專案升級成付費方案，或
- 請每位同事各自申請金鑰、各自部署一份

**通行碼是唯一的防護**，網址一旦外流又沒有通行碼，任何人都能消耗你的額度。
通行碼請用私訊傳，不要貼在公開群組。

---

## 本機測試雲端模式

推送前想先確認雲端模式正常，可以在本機模擬：

```bash
cd app
CLOUD=1 PORT=8080 ACCESS_CODE=test123 GEMINI_API_KEY=你的金鑰 node server.js
```

然後開 http://localhost:8080，應該會跳出通行碼輸入框。
