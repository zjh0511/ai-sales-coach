# 部署到雲端（讓同事直接用手機開網址測試）

目標：同事不用裝任何東西，開一個網址、輸入通行碼就能用。

以下用 **Render** 免費方案示範。Railway、Fly.io 等做法類似。

---

## 一次性設定（約 10 分鐘）

### 1. 註冊並連接 GitHub

到 https://render.com 用 GitHub 帳號註冊，授權它讀取這個 repo。

### 2. 建立服務

Render 後台 → **New** → **Blueprint** → 選這個 repo。
它會自動讀取根目錄的 `render.yaml`，不需要手動填 build 指令。

如果你想手動建立（New → Web Service），設定是：

| 欄位 | 值 |
|---|---|
| Root Directory | `app` |
| Build Command | 留空 |
| Start Command | `node server.js` |
| Health Check Path | `/api/health` |

### 3. 填入環境變數

在 Render 服務的 **Environment** 分頁新增：

| 變數 | 值 | 說明 |
|---|---|---|
| `CLOUD` | `1` | 讓伺服器跑純 HTTP，TLS 交給 Render |
| `ACCESS_CODE` | 自己取一組（選填） | 額外的一道門，擋住不相干的人打開網址 |

**不需要設定 API 金鑰。** 每位使用者在登入畫面輸入自己的金鑰，
伺服器只是轉送，不保存任何人的金鑰。

### 4. 部署完成

Render 會給你一個網址，例如 `https://ai-sales-coach.onrender.com`。

把網址和通行碼傳給同事，請他們用 **Safari 或 Chrome** 開啟，
然後「加入主畫面」，就會像一個 App。

---

## 給同事的說明（可以直接複製傳給他們）

> 網址：https://你的服務名稱.onrender.com
> 通行碼：（你設定的 ACCESS_CODE，沒設就不用）
>
> **開始前，請先申請一把自己的 AI 金鑰（免費）：**
> 1. 手機或電腦打開 https://aistudio.google.com/apikey
> 2. 用 Google 帳號登入 → 按 **Create API key**
> 3. 複製那一長串金鑰
>
> **然後：**
> 1. 用手機瀏覽器打開上面的網址。
> 2. 服務商選「Google Gemini」，把剛才複製的金鑰貼上去，按「驗證並登入」。
> 3. iPhone：點下方分享鍵 →「加入主畫面」，之後從桌面圖示開啟。
> 4. 第一次按麥克風時，請允許麥克風權限。
> 5. 語音練習時請盡量在安靜的地方，講完一句話停頓一下讓它辨識。
>
> 金鑰只會存在你自己的手機裡，用量算你自己的帳號，不會被別人看到。
> 如果是借別人的手機用，用完請按首頁下方的「登出／更換金鑰」。
>
> 提醒：這是訓練工具，內容僅供自學參考，請勿公開分享。
> 請不要輸入客戶的真實姓名、電話或身分證號。

---

## 必須知道的三件事

### 1. 免費方案會休眠

Render 免費方案在 15 分鐘沒人使用後會休眠，下一位使用者要等 **30～60 秒**才會醒來。
同事第一次打開覺得很慢是正常的，重新整理一次就好。付費方案（約 $7/月）沒有這個問題。

### 2. 上傳的文件不會永久保存

免費方案的檔案系統是暫時性的，每次重新部署或休眠喚醒後，
`app/data/` 裡上傳過的商品教材與保單條款**會消失，需要重新上傳**。

訓練紀錄存在每個人自己手機的瀏覽器裡，不受影響。

如果要讓文件永久保存，需要接資料庫或物件儲存，那是下一階段的事。

### 3. 每個人用自己的額度

每位使用者在登入畫面輸入自己的金鑰，用量算在各自的帳號上，
所以**不會有人消耗你的額度**，你也不需要在伺服器上放任何金鑰。

金鑰只存在各人自己的瀏覽器，隨每次請求送出，伺服器不寫入磁碟、不寫進日誌。

### 4. 上傳的文件是全體共用的

同一台伺服器上，任何人上傳的商品教材與保單條款，其他使用者都看得到、也都能刪除。
這對共用公司教材很方便，但**請勿上傳含客戶個資的文件**。
若需要每人獨立的文件空間，要再另外開發。

### 5. 本機測試雲端模式

```bash
cd app
CLOUD=1 PORT=8080 ACCESS_CODE=test123 node server.js
```

然後開 http://localhost:8080。

